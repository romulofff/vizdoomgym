# Lines starting with # are treated as comments (or with whitespaces+#).
# It doesn't matter if you use capital letters or not.
# It doesn't matter if you use underscore or camel notation for keys, e.g. episode_timeout is the same as episodeTimeout.

doom_scenario_path = hell_simple_sound_finder.wad

# Rewards
living_reward = -0.01
# living_reward = 0

# Rendering options
# screen_resolution = RES_640X480
screen_resolution = RES_320X240

screen_format = RGB24
render_hud = true
render_crosshair = false
render_weapon = true
render_decals = false
render_particles = false
window_visible = true

# Audio options
audio_buffer_enabled = true
game_args = "+snd_efx 0"

# make episodes start after 10 tics (after unholstering the gun)
episode_start_time = 10

# make episodes finish after 2100 actions (tics)
episode_timeout = 2100

# Available buttons
available_buttons = 
	{ 
		TURN_LEFT
		TURN_RIGHT
		MOVE_FORWARD 
        MOVE_BACKWARD
#		MOVE_LEFT
#		MOVE_RIGHT
	}

# Game variables that will be in the state
available_game_variables = { AMMO0 }

mode = PLAYER
doom_skill = 5
